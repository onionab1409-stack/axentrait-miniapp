# Отчёт: Экран 2 — Survey 1/5 (SCR-ONB-020)

**Дата:** 2026-02-25  
**Статус:** ⬜ → 🔶 Отчёт готов, правки не внесены  
**Файлы прототипа:** `прототип.html` → `#screen-2` (Survey Q1) и `#screen-3` (Survey Q5)  
**Файлы live-бота:** `features/onboarding/SurveyPage.tsx`, `components/layout/AppShell.tsx`, `components/ui/Card.tsx`

---

## 1. Общая концепция

### Прототип
Полноэкранный экран опроса с фоновым изображением (зелёная сетевая 3D-структура — нейросеть). Поверх фона: прогресс-точки, заголовок вопроса (cyan, weight 300), список опций (inline-flex с иконкой-эмодзи и label, стиль glass-morphism), кнопка "Далее" внизу (полупрозрачная). **Нет:** TopBar, Card-обёртки, subtitle, description опций.

### Live-бот
Стандартный AppShell-layout: TopBar "Подбор решения" со стрелкой назад, контент в Card-обёртке (тёмная непрозрачная карточка). Прогресс-точки, заголовок (крупный, жирный), subtitle, опции с icon + label + description, кнопка "Далее" (solid gradient primary). **Нет:** fullscreen bg, glass-morphism.

### Вердикт
**Принципиальное архитектурное расхождение.** Прототип — fullscreen immersive с glass-morphism. Live-бот — утилитарный card-based layout. Требуется значительная переработка.

---

## 2. Поэлементное сравнение

### 2.1 Общий лейаут

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Подход | **Fullscreen** с фоновым изображением | **Card-based** внутри AppShell | ❌ Архитектурное расхождение |
| Фон | 3D нейросетевая структура (зелёная/teal, fullscreen webp) | Тёмный gradient body (radial gradients) | ❌ |
| TopBar | **Нет** (только Telegram status bar) | Есть: "← Подбор решения" (TopBar компонент) | ❌ Лишний элемент |
| Card-обёртка | **Нет** — контент прямо на фоне | Есть: Card variant="default" с bg, border, shadow | ❌ Лишний элемент |
| Padding контента | `64px 20px 20px` (от верха, под status bar) | `16px` (Card padding) + `16px` (ax-shell) | ⚠️ |
| Overflow | `overflow-y: auto` на content-overlay | Scroll через ax-shell | ✅ |

**Рекомендация:** Для приведения к эталону нужно:
1. Убрать AppShell (TopBar + Card) на странице Survey
2. Сделать fullscreen layout как на Welcome (position: fixed, inset: 0)
3. Добавить fullscreen bg-изображение
4. Стилизовать контент поверх фона

---

### 2.2 Прогресс-индикатор (Dots)

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Тип | 5 dots (8px × 8px circles) | 5 dots (className `ax-dot`, 8px × 8px) | ✅ Структурно |
| Active dot | `background: #22D3EE` (заполненный cyan) | className `active` → `background: var(--app-accent)` | ⚠️ Проверить значение |
| Inactive dot | `rgba(255,255,255,0.15)` | className "" → `color-mix(var(--app-text-muted), 70%, transparent)` | ⚠️ |
| Current dot | Нет отличия от active | className `current` → полупрозрачный cyan с inset border | ⚠️ Дополнительное состояние |
| Шаг-счётчик | `1/5` справа, `font-size: 13px`, `color: rgba(126,232,242,0.4)` | `1/5` справа, `font-size: 12px`, className `ax-muted` | ⚠️ Близко |
| Расположение | `display:flex; gap:6px; margin-bottom:20px` | `className="ax-dots"` → `gap: var(--ax-space-2)` = 8px | ⚠️ gap 6→8 |

**Замечания:**
- Прототип не различает "active" и "current" — просто `#22D3EE` или серый. Live-бот добавляет 3-е состояние `current` с инверсной заливкой.
- Цвет счётчика: прототип `rgba(126,232,242,0.4)` (приглушённый teal), live `var(--app-text-muted)` (серый)

---

### 2.3 Заголовок вопроса (H2)

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Текст | "Какая ваша роль в компании?" | "Какая ваша роль в компании?" | ✅ |
| Тег | `<h2>` | `<h1 className="h2">` | ⚠️ Семантически |
| font-size | **`26px`** | **`24px`** (var(--ax-text-2xl)) | ⚠️ Расхождение |
| font-weight | **`300`** (тонкий) | **`700`** (жирный) | ❌ Критическое |
| color | **`#7EE8F2`** (cyan) | **`var(--app-text)`** = `#f0f6fc` (белый) | ❌ Критическое |
| letter-spacing | `0.5px` | `normal` | ⚠️ |
| text-shadow | `0 0 30px rgba(34,211,238,0.2)` | `none` | ❌ |
| margin-bottom | `8px` | `gap: 12px` (от контейнера) | ⚠️ |
| Перенос строк | `<br>` (принудительный) | Автоматический | ⚠️ |

---

### 2.4 Subtitle

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Наличие | **Нет** | **Есть** — `<p className="p muted">{subtitle}</p>` | ❌ Лишний элемент |
| Пример | — | "Фокус: бюджет и сроки или архитектура и стек" | — |

**Рекомендация:** Убрать subtitle или сделать его скрытым. В прототипе его нет.

---

### 2.5 Опции ответов

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Layout контейнера | `flex-direction:column; align-items:flex-start; flex:1; justify-content:space-between` | `className="ax-col"; gap:8px` | ⚠️ space-between vs gap |
| **Ширина опции** | **`inline-flex` (по контенту)** | **`width: 100%` (fullWidth)** | ❌ Расхождение |
| padding опции | `10px 14px` | `14px 16px` | ⚠️ Крупнее |
| border-radius | `14px` | `14px` | ✅ |
| **background (normal)** | **`rgba(0,0,0,0.18)`** | **`rgba(255,255,255,0.02)`** | ❌ Расхождение |
| **backdrop-filter** | **`blur(10px)`** (glass!) | **Нет** | ❌ Нет glass-morphism |
| border (normal) | `none` | `1px solid rgba(255,255,255,0.06)` | ⚠️ Есть border |
| background (selected) | `rgba(34,211,238,0.06)` | `rgba(34,211,238,0.06)` | ✅ |
| border (selected) | `none` | `1px solid rgba(34,211,238,0.25)` | ⚠️ Есть border |
| Emoji size | `22px` | `18px` | ⚠️ |
| Label font-size | `14px` | `14px` | ✅ |
| Label font-weight | `600` | `600` | ✅ |
| **Label color** | **`rgba(126,232,242,0.55)`** (приглушённый teal) | **`var(--app-text)`** = `#f0f6fc` (белый) | ❌ Расхождение |
| **Description** | **Нет** (только label) | **Есть** — className `option-hint` (12px, серый) | ❌ Лишний элемент |
| Transition | `all 0.15s ease` | `all 200ms ease` | ⚠️ |
| gap (icon-text) | `10px` | `12px` | ⚠️ |

**Критические отличия опций:**
1. **inline-flex vs fullWidth** — в прототипе опции по ширине контента, "воздушные", разбросаны по вертикали через `space-between`. В live — блоки на всю ширину, плотные.
2. **Glass-morphism** — в прототипе `backdrop-filter: blur(10px)` на чёрном полупрозрачном фоне, видно фоновое изображение сквозь опции. В live — плоский фон.
3. **Label цвет** — teal vs белый
4. **Description** — лишние в live

---

### 2.6 Кнопка "Далее"

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Текст | "Далее" (Q1-Q4), "Получить план" (Q5) | "Далее" / "Получить план" | ✅ |
| width | `100%` | `100%` (fullWidth) | ✅ |
| padding | `15px 0` | `16px 32px` | ⚠️ |
| border-radius | `14px` | `14px` | ✅ |
| **background** | **`rgba(34,211,238,0.15)`** (glass!) | **`linear-gradient(135deg, #22D3EE, #06B6D4, #0891B2)`** (solid) | ❌ Расхождение |
| **color** | **`#22D3EE`** (cyan text) | **`#050A0F`** (тёмный text) | ❌ Расхождение |
| border | `none` | `none` | ✅ |
| box-shadow | Нет | `0 4px 16px rgba(34,211,238,0.25), inset...` | ❌ Лишний |
| font-size | `15px` | `16px` | ⚠️ |
| font-weight | `600` | `600` | ✅ |
| margin-top | `16px` | `marginTop: 20` | ⚠️ |
| disabled state | Не показан | `opacity: 0.45` | ℹ️ Логика |

**Та же проблема, что и на Welcome** — кнопка "Далее" в прототипе glass-полупрозрачная, в live — яркий solid gradient.

---

### 2.7 Данные опций (Q1)

| Опция | Прототип (emoji+label) | Live (emoji+label+description) | Статус |
|-------|------------------------|-------------------------------|--------|
| 1 | 👔 CEO / Директор | 👔 CEO / Директор + "ROI, сроки, риски, бюджет, контроль" | ⚠️ +desc |
| 2 | 🧠 CTO / IT-директор | 🧠 CTO / IT-директор + "Архитектура, безопасность, интеграции, сроки, поддерживаемость" | ⚠️ +desc |
| 3 | ⚙️ COO / Операционный | ⚙️ COO / Операционный + "Цикл, качество, стабильность, загрузка, SLA" | ⚠️ +desc |
| 4 | 📌 Руководитель отдела | 📌 Руководитель отдела + "Пайплайны, очереди, автоматизация, метрики, SLA" | ⚠️ +desc |
| 5 | 📊 PM / Аналитик | 📊 PM / Аналитик + "Требования, данные, метрики, сценарии, сравнение" | ⚠️ +desc |
| 6 | ❓ Другое | ❓ Другое | ✅ |

**Замечание:** Прототип показывает только label без description. Live-бот добавляет description из JSON. Прототип имел в JS `d:'ROI, сроки, риски, бюджет'`, но НЕ рендерил их в DOM.

---

## 3. Сводка расхождений

### Критические (❌) — Архитектурные
1. **Card-based layout vs fullscreen** — Survey в прототипе это fullscreen immersive (как Welcome), в live — утилитарная карточка
2. **Нет фонового изображения** — прототип имеет 3D нейросетевую текстуру
3. **TopBar "Подбор решения"** — отсутствует в прототипе (навигация через Telegram back)

### Критические (❌) — Стилевые
4. **H2 font-weight: 700 → 300** — жирный вместо тонкого
5. **H2 color: white → #7EE8F2** (cyan)
6. **H2 text-shadow отсутствует**
7. **Опции inline-flex → fullWidth** — должны быть по ширине контента
8. **Опции: нет backdrop-filter: blur(10px)** — glass-morphism
9. **Опции background: rgba(255,255,255,0.02) → rgba(0,0,0,0.18)**
10. **Опции label color: white → rgba(126,232,242,0.55)** (teal)
11. **Кнопка "Далее": solid gradient → полупрозрачная**

### Средние (⚠️)
12. **Subtitle** — есть в live, нет в прототипе
13. **Description** в опциях — есть в live, нет в прототипе
14. H2 font-size: 24px → 26px
15. Dots gap: 8px → 6px
16. Счётчик цвет: серый → teal приглушённый
17. Emoji size: 18px → 22px
18. Опции border → none в прототипе
19. Padding различия

### Информационные (ℹ️)
20. Live загружает вопросы через API (fetchOnboardingConfig), прототип — статичный
21. Telegram Haptic feedback при выборе опции
22. Анимация `ax-step-slide-left` при смене шагов

---

## 4. План правок

### Шаг 1: Переработать SurveyPage — fullscreen layout

Заменить AppShell+Card на fullscreen layout аналогично WelcomePage:

```tsx
export default function SurveyPage() {
  // ... existing logic ...
  
  return (
    <div style={{ position: 'fixed', inset: 0, display: 'flex', flexDirection: 'column', overflow: 'hidden' }}>
      {/* Fullscreen background */}
      <div style={{ position: 'absolute', inset: 0, zIndex: 0 }}>
        <MjImage id="survey-bg" height="100%" borderRadius={0} scrim={false} alt="Survey bg" />
      </div>

      {/* Content overlay */}
      <div style={{
        position: 'relative', zIndex: 1,
        height: '100%', display: 'flex', flexDirection: 'column',
        padding: '64px 20px 20px',
        overflowY: 'auto',
      }}>
        {/* Progress dots + step counter */}
        {/* H2 title */}
        {/* Options */}
        {/* Button */}
      </div>
    </div>
  );
}
```

### Шаг 2: Исправить H2

```tsx
<h2 style={{
  fontSize: 26,
  fontWeight: 300,
  marginBottom: 8,
  letterSpacing: '0.5px',
  color: '#7EE8F2',
  textShadow: '0 0 30px rgba(34,211,238,0.2)',
}}>
  {currentQuestion.title}
</h2>
```

### Шаг 3: Убрать subtitle
```diff
- <p className="p muted">{currentQuestion.subtitle}</p>
```

### Шаг 4: Исправить опции
```tsx
function optionStyle(isSelected: boolean): React.CSSProperties {
  return {
    display: 'inline-flex',
    alignItems: 'center',
    gap: 10,
    padding: '10px 14px',
    borderRadius: 14,
    background: isSelected ? 'rgba(34,211,238,0.06)' : 'rgba(0,0,0,0.18)',
    backdropFilter: 'blur(10px)',
    WebkitBackdropFilter: 'blur(10px)',
    border: 'none',
    cursor: 'pointer',
    transition: 'all 0.15s ease',
    textAlign: 'left',
    color: 'var(--app-text)',
    // НЕ width: 100% — по контенту
  };
}
```

### Шаг 5: Скрыть description, исправить label color
```tsx
<span style={{ fontSize: 22, lineHeight: 1 }}>{option.icon}</span>
<span style={{
  fontSize: 14,
  fontWeight: 600,
  color: 'rgba(126,232,242,0.55)',
}}>
  {option.label}
</span>
{/* НЕ рендерить option.description */}
```

### Шаг 6: Кнопка "Далее" — glass-стиль
```tsx
<Button
  variant="primary" size="lg" fullWidth
  disabled={!canContinue}
  onClick={() => void next()}
  style={{
    marginTop: 16,
    background: 'rgba(34,211,238,0.15)',
    color: '#22D3EE',
    boxShadow: 'none',
    border: 'none',
    padding: '15px 0',
    fontSize: 15,
  }}
>
  {actionLabel}
</Button>
```

### Шаг 7: Добавить фоновое изображение для Survey
В `imageMap.ts`:
```diff
+ 'survey-bg': '/images/heroes/survey-bg.webp',
```
В `FALLBACK_GRADIENTS`:
```diff
+ 'survey-bg': 'radial-gradient(ellipse at 50% 30%, rgba(34,211,238,0.12), rgba(5,10,15,0.95) 70%)',
```

### Шаг 8: Опции контейнер — space-between
```tsx
<div style={{
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'flex-start',
  justifyContent: 'space-between',
  flex: 1,
  padding: '10px 0',
}}>
```

---

## 5. CSS-токены экрана (из прототипа)

```css
/* Survey Screen Tokens */
--survey-bg: url(...);                            /* 3D neural network texture */
--survey-padding: 64px 20px 20px;

--survey-dot-size: 8px;
--survey-dot-active: #22D3EE;
--survey-dot-inactive: rgba(255,255,255,0.15);
--survey-dot-gap: 6px;
--survey-counter-size: 13px;
--survey-counter-color: rgba(126,232,242,0.4);

--survey-h2-size: 26px;
--survey-h2-weight: 300;
--survey-h2-color: #7EE8F2;
--survey-h2-spacing: 0.5px;
--survey-h2-shadow: 0 0 30px rgba(34,211,238,0.2);
--survey-h2-margin-bottom: 8px;

--survey-option-display: inline-flex;
--survey-option-padding: 10px 14px;
--survey-option-radius: 14px;
--survey-option-bg: rgba(0,0,0,0.18);
--survey-option-bg-selected: rgba(34,211,238,0.06);
--survey-option-blur: blur(10px);
--survey-option-border: none;
--survey-option-transition: all 0.15s ease;

--survey-option-emoji-size: 22px;
--survey-option-label-size: 14px;
--survey-option-label-weight: 600;
--survey-option-label-color: rgba(126,232,242,0.55);

--survey-btn-bg: rgba(34,211,238,0.15);
--survey-btn-color: #22D3EE;
--survey-btn-radius: 14px;
--survey-btn-padding: 15px 0;
--survey-btn-size: 15px;
--survey-btn-weight: 600;
--survey-btn-margin-top: 16px;
```

---

## 6. Примечание: Survey 5/5 (screen-3)

Survey 5/5 в прототипе идентичен по стилю Survey 1/5, отличается только:
- Все 5 точек `#22D3EE` (все active)
- Счётчик `5/5`
- Заголовок: "Какая ваша основная цель?"
- Другие опции (💰 Снизить затраты, ⚡ Ускорить цикл и т.д.)
- Кнопка: "Получить план" вместо "Далее"
- Фоновое изображение тоже может отличаться (bg-survey5 использует heroAiHub)

Все стилевые правки применяются одинаково для всех шагов Survey.
