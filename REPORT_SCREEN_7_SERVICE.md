# Отчёт: Экран 7 — Service Detail (SCR-SVC-020)

**Дата:** 2026-02-25  
**Статус:** 🔶 → 🔶 Отчёт готов, правки не внесены  
**Файлы прототипа:** `прототип.html` → `#screen-7`  
**Файлы live-бота:** `features/services/ServiceDetailPage.tsx`, `components/ui/MjImage.tsx`, `components/ui/Chip.tsx`

---

## 1. Общая концепция

### Прототип
Fullscreen-экран с фоновым изображением (3D нейросетевая структура). Контент позиционирован **снизу** (`justify-content: flex-end`):
1. **H1** — "RPA и интеграции CRM/ERP" (22px, **weight 700**, white)
2. **Subtitle** — "Роботы и интеграции, которые снимают рутину" (13px, muted)
3. **Chip-бейджи** — "4-8 недель", "от 250 000 ₽"
4. **Описание** — 2 абзаца (14px, muted, line-height 1.6)
5. **"Оставить заявку"** — btn-primary (solid gradient!)
6. **"Все услуги"** — btn-secondary (transparent, cyan border)
7. **Bottom Nav** — 4 элемента, "Услуги" active

Фоновое изображение занимает **всю верхнюю половину** экрана, текст начинается примерно с середины.

**Уникальная особенность:** Это единственный экран в прототипе, где btn-primary использует **solid gradient** (не glass!). На Welcome и Survey кнопки glass — `rgba(34,211,238,0.15)`. На Service — полноценный `linear-gradient(135deg, #22D3EE, #06B6D4)` с box-shadow. Это **сознательное решение** — CTA "Оставить заявку" должна выделяться.

### Live-бот
AppShell "← Услуга" + showBottomNav. Развёрнутая структура:
1. **Hero section** — MjImage (196px) + H1 (24px, bold) + subtitle + chips в отдельной карточке
2. **Card default** — Описание (несколько абзацев)
3. **Card glass** — "Что вы получите" (deliverables список с ✓)
4. **Card default** — "Предпосылки к старту" (prerequisites список с •)
5. **Card glass** — "Пакеты" + кнопки "Сравнить пакеты" и "Рассчитать ROI"
6. **Секция** — "Кейсы" + CaseCard
7. **"Оставить заявку"** — btn-primary (solid gradient)
8. **"Все услуги"** — btn-secondary
9. **Bottom Nav** — 5 элементов

### Вердикт
**Структурное расхождение в объёме контента**, аналогично Result. Прототип — компактный fullscreen лендинг. Live — информационно насыщенная страница с 5+ секциями. Стилистически же это **наиболее близкий экран**, т.к. btn-primary совпадает по стилю!

---

## 2. Поэлементное сравнение

### 2.1 Лейаут

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Подход | **Fullscreen** с фоном, контент снизу | **Card-based** (AppShell) | ❌ Архитектурное |
| Фон | Fullscreen bg-image (3D нейросеть) | Без fullscreen bg | ❌ |
| TopBar | **Нет** | "← Услуга" | ❌ Лишний |
| Контент alignment | `justify-content: flex-end` (снизу) | Сверху вниз | ❌ |
| Padding | `64px 20px 80px` | Card padding + shell | ⚠️ |
| Bottom Nav | Есть (4 элемента) | Есть (5 элементов) | ⚠️ |

---

### 2.2 Hero / Image

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Тип | **Fullscreen background-image** | MjImage (196px, в отдельной section) | ❌ |
| Размер | 100% экрана | 196px (фиксированная высота) | ❌ |
| border-radius | 0 (fullscreen) | 18px (section) | ⚠️ |
| Scrim/Overlay | Нет (контент внизу) | scrim={false} | ✅ |

---

### 2.3 Заголовок H1

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| font-size | **`22px`** | **`24px`** | ⚠️ |
| font-weight | `700` | `700` | ✅ 🎉 |
| line-height | `1.2` | `1.2` | ✅ |
| color | **`#f0f6fc` (white, inherited)** | **`#f0f6fc` (white)** | ✅ 🎉 |
| margin-bottom | `6px` | `8px` | ⚠️ |

**🎉 Заголовок — практически совпадает!** Это единственный экран, где и прототип, и live используют weight 700 + white цвет.

---

### 2.4 Subtitle

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| font-size | `13px` | `14px` | ⚠️ |
| color | `rgba(240,246,252,0.5)` | `rgba(240,246,252,0.6)` | ⚠️ |
| line-height | inherited | `1.5` | ⚠️ |
| margin-bottom | `12px` | — (padding from parent) | ⚠️ |

---

### 2.5 Chips (Timeline + Price)

| Параметр | Прототип | Live-бот (Chip) | Статус |
|----------|----------|-----------------|--------|
| font-size | `12px` | `12px` | ✅ |
| padding | `5px 12px` | `6px 12px` | ⚠️ Близко |
| border-radius | `8px` | `8px` | ✅ |
| color | `#F0F6FC` (white) | `rgba(240,246,252,0.65)` | ⚠️ Бледнее |
| background | `rgba(255,255,255,0.1)` (chip class) | `rgba(255,255,255,0.04)` | ⚠️ |
| backdrop-filter | `blur(4px)` | Нет | ⚠️ |
| gap | `8px` | `8px` | ✅ |
| margin-bottom | `20px` | `marginTop: 16` | ⚠️ |

---

### 2.6 Описание

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| font-size | `14px` | `15px` | ⚠️ |
| color | `rgba(240,246,252,0.65)` | `rgba(240,246,252,0.72)` | ⚠️ |
| line-height | `1.6` | `1.6` | ✅ |
| Container | Прямо в content-overlay | Card variant="default" | ⚠️ |
| margin-bottom | `14px` / `20px` | `16px` | ⚠️ |

---

### 2.7 Кнопка "Оставить заявку" (btn-primary)

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| width | `100%` | `100%` (fullWidth) | ✅ |
| padding | `15px 0` | `16px 32px` | ⚠️ |
| border-radius | `14px` | `14px` | ✅ |
| **background** | **`linear-gradient(135deg, #22D3EE, #06B6D4)`** | **`linear-gradient(135deg, #22D3EE, #06B6D4, #0891B2)`** | ✅ Близко! 🎉 |
| **color** | **`#050A0F`** (тёмный) | **`#050A0F`** | ✅ 🎉 |
| font-size | `15px` | `16px` | ⚠️ |
| font-weight | `600` | `600` | ✅ |
| box-shadow | `0 4px 20px rgba(34,211,238,0.25)` | `0 4px 16px rgba(34,211,238,0.25), inset...` | ⚠️ Близко |
| margin-bottom | `10px` | `marginTop: 8; marginBottom: 8` | ⚠️ |

**🎉 Кнопка "Оставить заявку" — наилучшее совпадение среди всех экранов!** Solid gradient на обеих версиях.

---

### 2.8 Кнопка "Все услуги" (btn-secondary)

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| width | `100%` (inline style) | `100%` (fullWidth) | ✅ |
| padding | `12px 0` (inline) / `11px 22px` (class) | `16px 32px` | ⚠️ |
| border-radius | `12px` | `14px` | ⚠️ |
| background | `transparent` | `transparent` | ✅ |
| color | `#22D3EE` | `#22D3EE` | ✅ |
| border | `1px solid rgba(34,211,238,0.3)` | `1px solid rgba(34,211,238,0.15)` | ⚠️ |
| font-size | `13px` | `16px` | ⚠️ |
| font-weight | `500` | `600` | ⚠️ |

---

### 2.9 Секции, ОТСУТСТВУЮЩИЕ в прототипе

| Секция | Live-бот | Прототип | Статус |
|--------|----------|----------|--------|
| "Что вы получите" (Card glass + deliverables) | ✅ | ❌ | Лишний |
| "Предпосылки к старту" (Card default + prerequisites) | ✅ | ❌ | Лишний |
| "Пакеты" (Card glass + кнопки) | ✅ | ❌ | Лишний |
| "Кейсы" (секция + CaseCard) | ✅ | ❌ | Лишний |

---

### 2.10 Bottom Navigation

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Наличие | ✅ | ✅ | ✅ |
| Active tab | **"Услуги"** | **"Услуги"** | ✅ |
| Количество | 4 | 5 (+Профиль) | ⚠️ |

---

## 3. Сводка расхождений

### Критические (❌) — Архитектурные
1. **Fullscreen bg vs card-based** — прототип: изображение на весь экран, контент снизу. Live: section + cards.
2. **Лишние секции** — deliverables, prerequisites, packages, related cases — 4 блока отсутствуют в прототипе

### Средние (⚠️)
3. H1 font-size: 24 → 22px
4. Subtitle font-size: 14 → 13px
5. Description font-size: 15 → 14px
6. Chip color brightness
7. btn-secondary font-size/weight/border
8. BottomNav: 5 vs 4 элемента

### Совпадения (✅) — Лучший результат!
9. **H1 weight 700** — совпадает! (единственный экран!)
10. **H1 color white** — совпадает!
11. **btn-primary solid gradient** — совпадает! 🎉
12. **btn-primary color dark** — совпадает!
13. Chip layout и border-radius
14. Description line-height 1.6
15. Bottom Nav active "Услуги"

---

## 4. Вопросы к заказчику

> **Тот же вопрос что и для Result:** Live-бот значительно богаче прототипа (deliverables, prerequisites, packages, related cases). Варианты:
> 1. Строго по прототипу — убрать 4 секции
> 2. Стиль прототипа + контент live (рекомендуется)
> 3. Обновить прототип

---

## 5. План правок (Вариант 2)

### Шаг 1: Fullscreen layout
Заменить hero section на fullscreen bg:
```tsx
<div style={{ position: 'relative', minHeight: '45vh' }}>
  <MjImage id="bg-image" style={{ position: 'absolute', inset: 0, objectFit: 'cover' }} />
</div>
```

### Шаг 2: Мелкие стилевые правки
```tsx
// H1: 22px (было 24)
<h1 style={{ fontSize: 22, fontWeight: 700, lineHeight: 1.2 }}>{service.title}</h1>

// Subtitle: 13px (было 14)
<p style={{ fontSize: 13, color: 'rgba(240,246,252,0.5)' }}>{service.shortPitch}</p>

// Description: 14px (было 15)
// color: rgba(240,246,252,0.65) (было 0.72)
```

### Шаг 3: btn-secondary — компактнее
```tsx
<Button variant="secondary" size="lg" fullWidth
  onClick={() => navigate('/services')}
  style={{
    fontSize: 13,
    fontWeight: 500,
    borderRadius: 12,
    borderColor: 'rgba(34,211,238,0.3)',
    padding: '12px 0',
  }}
>
  Все услуги
</Button>
```

---

## 6. CSS-токены экрана (из прототипа)

```css
/* Service Detail Tokens */
--svc-bg: fullscreen image (heroAiHub);
--svc-padding: 64px 20px 80px;
--svc-content-align: flex-end; /* content at bottom */

--svc-h1-size: 22px;
--svc-h1-weight: 700;        /* EXCEPTION: bold on this screen! */
--svc-h1-line-height: 1.2;
--svc-h1-color: inherit (#f0f6fc);
--svc-h1-margin-bottom: 6px;

--svc-subtitle-size: 13px;
--svc-subtitle-color: rgba(240,246,252,0.5);
--svc-subtitle-margin-bottom: 12px;

--svc-chip-size: 12px;
--svc-chip-padding: 5px 12px;
--svc-chip-color: #F0F6FC;
--svc-chip-bg: rgba(255,255,255,0.1);
--svc-chip-radius: 8px;
--svc-chip-blur: blur(4px);
--svc-chip-gap: 8px;
--svc-chip-margin-bottom: 20px;

--svc-desc-size: 14px;
--svc-desc-color: rgba(240,246,252,0.65);
--svc-desc-line-height: 1.6;

/* Primary CTA — SOLID gradient (exception to glass pattern!) */
--svc-primary-bg: linear-gradient(135deg, #22D3EE, #06B6D4);
--svc-primary-color: #050A0F;
--svc-primary-radius: 14px;
--svc-primary-padding: 15px 0;
--svc-primary-size: 15px;
--svc-primary-weight: 600;
--svc-primary-shadow: 0 4px 20px rgba(34,211,238,0.25);

/* Secondary CTA */
--svc-secondary-bg: transparent;
--svc-secondary-color: #22D3EE;
--svc-secondary-border: 1px solid rgba(34,211,238,0.3);
--svc-secondary-radius: 12px;
--svc-secondary-size: 13px;
--svc-secondary-weight: 500;
--svc-secondary-padding: 12px 0;
```

---

## 7. Замечание: Два стиля btn-primary в прототипе

Прототип использует **два разных стиля** для primary-кнопки:

| Экран | Стиль btn-primary | Назначение |
|-------|-------------------|------------|
| Welcome | `rgba(34,211,238,0.15)` (glass) + cyan text | Навигационная CTA |
| Survey 1/5 | `rgba(34,211,238,0.15)` (glass) + cyan text | Навигационная CTA |
| Survey 5/5 | `rgba(34,211,238,0.15)` (glass) + cyan text | Навигационная CTA |
| Result | `rgba(34,211,238,0.15)` (glass) + cyan text | AI-button (вторичный CTA) |
| **Service** | **`linear-gradient` (solid)** + **dark text** | **Конверсионный CTA** |

Это **сознательная дизайнерская система**: glass-кнопки для навигации, solid gradient — для конверсии ("Оставить заявку"). Live-бот использует solid gradient **везде**, размывая это различие.
