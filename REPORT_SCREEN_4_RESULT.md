# Отчёт: Экран 4 — Result (SCR-ONB-030)

**Дата:** 2026-02-25  
**Статус:** ⬜ → 🔶 Отчёт готов, правки не внесены  
**Файлы прототипа:** `прототип.html` → `#screen-4`  
**Файлы live-бота:** `features/onboarding/OnboardingResultPage.tsx`, `components/domain/ServiceCard.tsx`, `components/domain/CaseCard.tsx`, `components/layout/BottomNav.tsx`

---

## 1. Общая концепция

### Прототип
Минималистичный экран на тёмном фоне (`#050A0F`, без фонового изображения). Структура:
1. **H2** — "Рекомендации под вашу задачу" (cyan, weight 300)
2. **3 карточки-услуги** (mj-card) — fullscreen-image карточки с названием услуги, равномерно распределены по вертикали через `space-evenly`
3. **Кнопка** — "Задайте вопрос искусственному интеллекту" (полупрозрачная, приглушённый cyan)
4. **Bottom Navigation** — 4 элемента: Услуги, Кейсы, AI-демо, Заявка

**Нет:** TopBar, Card-обёрток, описания, subtitle, секций "Похожий кейс", "AI-демо", "Обсудить внедрение", "Смотреть всё без фильтра". Очень лаконичный экран.

### Live-бот
Развёрнутый результат с множеством секций:
1. **AppShell** — TopBar "← Результат"
2. **Card** — Headline + description
3. **Card elevated** — "Подходящие услуги" + 2 ServiceCard (с category, title, description, timeline, price)
4. **Button secondary** — "Задайте вопрос искусственному интеллекту"
5. **Card glass** — "Похожий кейс" + CaseCard
6. **Card interactive** — "Попробовать AI-демо" + кнопка "Запустить демо"
7. **Card interactive** — "Обсудить внедрение" + кнопка "Оставить заявку"
8. **Button ghost** — "Смотреть всё без фильтра"

**Нет:** Bottom Navigation (showBottomNav не передан)

### Вердикт
**Принципиальное расхождение в объёме контента и структуре.** Прототип — визуально-минималистичный, 3 карточки-картинки + 1 кнопка + навигация. Live-бот — информационно насыщенный, 5+ секций, множество CTA. Структурная переработка.

---

## 2. Поэлементное сравнение

### 2.1 Лейаут

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| TopBar | **Нет** | "← Результат" (AppShell) | ❌ Лишний |
| Фон | `#050A0F` (тёмный solid) | Body gradients | ⚠️ Близко |
| Card-обёртки | **Нет** | Множество Card (default, elevated, glass, interactive) | ❌ Лишние |
| Padding | `64px 20px 80px` | Card padding 16px + shell padding | ⚠️ |
| Bottom Nav | **Есть** (4 элемента) | **Нет** (showBottomNav не передан) | ❌ Отсутствует |
| Scroll | `overflow-y: auto` на content-overlay | Через ax-shell | ✅ |

---

### 2.2 Заголовок

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Текст | "Рекомендации под вашу задачу" | Из config `resultConfig.headline` | ✅ Контент |
| font-size | `26px` | `24px` (h2 class) | ⚠️ |
| font-weight | **`300`** | **`700`** | ❌ |
| color | **`#7EE8F2`** | **`#f0f6fc` (white)** | ❌ |
| letter-spacing | `0.5px` | `normal` | ⚠️ |
| text-shadow | `0 0 30px rgba(34,211,238,0.2)` | `none` | ❌ |
| margin-bottom | `6px` | `gap: 10px` (от контейнера) | ⚠️ |

---

### 2.3 Description / Subtitle

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Наличие | **Нет** | **Есть** — "Мы подобрали услуги, кейс и AI-демо на основе ваших ответов." | ❌ Лишний |

---

### 2.4 Карточки услуг (mj-card)

**Прототип:** 3 карточки с background-image, равномерно распределены (`justify-content: space-evenly`, `flex:1`). Каждая — изображение на весь фон, название услуги поверх (15px, weight 300, cyan #7EE8F2).

**Live:** 2 ServiceCard компонента с category label, title (17px, bold #fff), description, timeline badge, price badge.

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| **Количество** | **3** | **2** (slice(0,2)) | ⚠️ |
| **Лейаут** | `flex:1; space-evenly` (заполняют экран) | Вертикально в Card, фиксированная высота | ❌ |
| border-radius | `18px` | `16px` | ⚠️ |
| border | `1px solid rgba(255,255,255,0.06)` | `1px solid var(--app-border)` | ✅ Близко |
| background | **Fullscreen image** (`background-size: cover`) | MjImage component (160px height) | ⚠️ |
| **Название: font-size** | **`15px`** | **`17px`** | ⚠️ |
| **Название: font-weight** | **`300`** (тонкий) | **`700`** (жирный) | ❌ |
| **Название: color** | **`#7EE8F2`** (cyan) | **`#fff`** (white) | ❌ |
| Название: letter-spacing | `0.5px` | `normal` | ⚠️ |
| Название: margin-bottom | `4px` | `4px` | ✅ |
| **Category label** | **Нет** | Есть (11px, muted) | ❌ Лишний |
| **Description** | **Нет** | Есть (12px, muted) | ❌ Лишний |
| **Badges (timeline, price)** | **Нет** | Есть (Badge компонент) | ❌ Лишний |
| Содержимое | Только название | Category + title + description + badges | ❌ |
| **Секция-обёртка** | **Нет** (прямо в контенте) | Card variant="elevated" + h2 "Подходящие услуги" | ❌ Лишний |

**Карточки прототипа (контент):**
1. "Внедрение AI" → image: svcAi
2. "IoT и цифровые двойники" → image: svcIot
3. "RPA и интеграции CRM/ERP" → image: svcAutomation

---

### 2.5 Кнопка "Задайте вопрос ИИ"

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Текст | "Задайте вопрос искусственному интеллекту" | "Задайте вопрос искусственному интеллекту" | ✅ |
| width | `100%` | `100%` (fullWidth) | ✅ |
| padding | `15px 0` | `16px 32px` | ⚠️ |
| border-radius | `14px` | `14px` | ✅ |
| **background** | **`rgba(34,211,238,0.15)`** (glass) | **secondary variant** (transparent + border) | ⚠️ Близко, но не glass |
| **color** | **`rgba(34,211,238,0.5)`** (приглушённый cyan!) | **`#22D3EE`** (яркий cyan) | ❌ Расхождение |
| font-size | `14px` | `16px` | ⚠️ |
| font-weight | `600` | `600` | ✅ |
| border | `none` | `1px solid rgba(34,211,238,0.15)` | ⚠️ |

**Важно:** В прототипе текст кнопки **приглушённый** `rgba(34,211,238,0.5)` — это сознательное дизайнерское решение: кнопка AI — второстепенный CTA, не отвлекает от основных карточек.

---

### 2.6 Секции, ОТСУТСТВУЮЩИЕ в прототипе

Следующие блоки есть **только в live-боте** и отсутствуют в прототипе:

| Секция | Live-бот | Прототип | Статус |
|--------|----------|----------|--------|
| "Подходящие услуги" (h2 + Card elevated обёртка) | ✅ | ❌ | Лишний |
| Description "Мы подобрали..." | ✅ | ❌ | Лишний |
| "Похожий кейс" (Card glass + CaseCard) | ✅ | ❌ | Лишний |
| "Попробовать AI-демо" (Card interactive + кнопка) | ✅ | ❌ | Лишний |
| "Обсудить внедрение" + "Оставить заявку" (Card interactive) | ✅ | ❌ | Лишний |
| "Смотреть всё без фильтра" (Button ghost) | ✅ | ❌ | Лишний |

**Вердикт:** Live-бот содержит значительно больше секций. Прототип показывает только 3 карточки + 1 кнопку. Вопрос к заказчику: убирать лишние секции или дополнять прототип?

---

### 2.7 Bottom Navigation

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Наличие на Result | **Есть** | **Нет** (showBottomNav не передан) | ❌ |
| Элементы | 4: Услуги, Кейсы, AI-демо, Заявка | 5: Услуги, Кейсы, AI-демо, Заявка, **Профиль** | ⚠️ Лишний 5-й |
| Иконки | Unicode символы (◇, ⊞, ◈, ▷) | Lucide icons (Diamond, Grid3X3, DiamondFilled, Play, User) | ⚠️ |
| height | `64px` | `64px` | ✅ |
| background | `rgba(5,10,15,0.7)` | `linear-gradient(180deg, rgba(5,10,15,0.85), rgba(5,10,15,0.98))` | ⚠️ Близко |
| backdrop-filter | `blur(20px)` | `blur(20px)` | ✅ |
| border-top | `1px solid rgba(255,255,255,0.06)` | `1px solid rgba(255,255,255,0.06)` | ✅ |
| Label font-size | `10px` | `11px` | ⚠️ |
| Inactive color | `rgba(240,246,252,0.35)` | `rgba(240,246,252,0.38)` | ✅ Близко |
| Active color | `#22D3EE` | `#22D3EE` | ✅ |
| Active dot | Нет | Есть (4px cyan dot) | ⚠️ Допустимо |
| Active glow | Нет | `drop-shadow(0 0 8px rgba(34,211,238,0.4))` | ⚠️ Допустимо |
| Active state | Ни один не active на Result | — | ℹ️ |

**Рекомендация:** Добавить `showBottomNav` на OnboardingResultPage. Убрать пятый элемент "Профиль" или уточнить с заказчиком.

---

## 3. Сводка расхождений

### Критические (❌) — Архитектурные
1. **Лишние секции** — live содержит 5+ дополнительных блоков, отсутствующих в прототипе
2. **AppShell + Card обёртки** — прототип не имеет TopBar и Card
3. **Bottom Navigation отсутствует** — в live нет BottomNav на Result, в прототипе есть

### Критические (❌) — Стилевые (повторяющийся паттерн)
4. **H2 font-weight: 700 → 300**
5. **H2 color: white → #7EE8F2**
6. **H2 text-shadow отсутствует**
7. **Карточки: title weight 700 → 300**
8. **Карточки: title color #fff → #7EE8F2**
9. **Карточки: лишние elements** (category, description, badges)
10. **Кнопка AI: яркий → приглушённый** `rgba(34,211,238,0.5)`

### Средние (⚠️)
11. Количество карточек: 2 → 3
12. Карточки layout: фиксированная высота → flex:1 space-evenly
13. font-size различия (26/24, 15/17, 14/16)
14. Description (subtitle) — лишний в live
15. BottomNav: 5 vs 4 элемента

---

## 4. Вопросы к заказчику

> ⚠️ **ВАЖНЫЙ ВОПРОС:** Экран Result в live-боте значительно **богаче** прототипа. Прототип показывает минимум: 3 карточки + кнопка ИИ. Live-бот добавляет: кейс, AI-демо секцию, CTA заявки, skip-кнопку.
>
> **Варианты:**
> 1. **Строго по прототипу** — убрать все лишние секции (потеря функционала)
> 2. **Стиль прототипа + контент live** — оставить секции, но стилизовать в формате прототипа (mj-card, glass, weight 300, cyan)
> 3. **Обновить прототип** — признать live-версию более полной и обновить эталон
>
> Рекомендация: **Вариант 2** — оптимальный баланс.

---

## 5. План правок (Вариант 2 — стиль прототипа, контент live)

### Шаг 1: Добавить BottomNav
```tsx
<AppShell title="Результат" showBack showBottomNav>
```

### Шаг 2: Убрать TopBar (или сделать прозрачный)
Для полного соответствия — заменить AppShell на fullscreen layout. Для Варианта 2 — можно оставить AppShell но с прозрачным TopBar.

### Шаг 3: Исправить H2
```tsx
<h2 style={{
  fontSize: 26,
  fontWeight: 300,
  marginBottom: 6,
  letterSpacing: '0.5px',
  color: '#7EE8F2',
  textShadow: '0 0 30px rgba(34,211,238,0.2)',
}}>
  {resultConfig.headline}
</h2>
```

### Шаг 4: Убрать description
```diff
- <p className="p muted">{resultConfig.description}</p>
```

### Шаг 5: ServiceCard — стиль mj-card
```tsx
// ServiceCard.tsx — упрощение под прототип
<div style={{
  borderRadius: 18,
  overflow: 'hidden',
  position: 'relative',
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'flex-end',
  padding: 16,
  border: '1px solid rgba(255,255,255,0.06)',
  flex: 1,
  minHeight: 0,
  backgroundSize: 'cover',
  backgroundPosition: 'center',
}}>
  {/* Убрать category, description, badges */}
  <div style={{
    fontSize: 15,
    fontWeight: 300,
    marginBottom: 4,
    letterSpacing: '0.5px',
    color: '#7EE8F2',
  }}>
    {service.title}
  </div>
</div>
```

### Шаг 6: Карточки — flex:1 space-evenly
```tsx
<div style={{
  flex: 1,
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'space-evenly',
}}>
  {recommendedServices.map(svc => <ServiceCard key={svc.id} ... />)}
</div>
```

### Шаг 7: Кнопка AI — приглушённая
```tsx
<Button
  fullWidth
  onClick={() => navigate('/ai')}
  style={{
    padding: '15px 0',
    borderRadius: 14,
    border: 'none',
    background: 'rgba(34,211,238,0.15)',
    color: 'rgba(34,211,238,0.5)',
    fontSize: 14,
    fontWeight: 600,
    boxShadow: 'none',
  }}
>
  Задайте вопрос искусственному интеллекту
</Button>
```

---

## 6. CSS-токены экрана (из прототипа)

```css
/* Result Screen Tokens */
--result-bg: #050A0F;
--result-padding: 64px 20px 80px;

--result-h2-size: 26px;
--result-h2-weight: 300;
--result-h2-color: #7EE8F2;
--result-h2-spacing: 0.5px;
--result-h2-shadow: 0 0 30px rgba(34,211,238,0.2);
--result-h2-margin-bottom: 6px;

/* mj-card (service recommendation card) */
--result-card-radius: 18px;
--result-card-border: 1px solid rgba(255,255,255,0.06);
--result-card-padding: 16px;
--result-card-margin-bottom: 10px;
--result-card-bg: cover image;
--result-card-layout: flex:1; justify-content:flex-end;

--result-card-title-size: 15px;
--result-card-title-weight: 300;
--result-card-title-color: #7EE8F2;
--result-card-title-spacing: 0.5px;

/* AI CTA button */
--result-ai-btn-bg: rgba(34,211,238,0.15);
--result-ai-btn-color: rgba(34,211,238,0.5); /* deliberately muted! */
--result-ai-btn-radius: 14px;
--result-ai-btn-padding: 15px 0;
--result-ai-btn-size: 14px;
--result-ai-btn-weight: 600;

/* Bottom Nav */
--nav-height: 64px;
--nav-bg: rgba(5,10,15,0.7);
--nav-blur: blur(20px);
--nav-border-top: 1px solid rgba(255,255,255,0.06);
--nav-label-size: 10px;
--nav-inactive-color: rgba(240,246,252,0.35);
--nav-active-color: #22D3EE;
--nav-items: Услуги | Кейсы | AI-демо | Заявка (4 шт.)
```
