# Отчёт: Экран 0 — Splash (SCR-ONB-001)

**Дата:** 2026-02-25  
**Статус:** ⬜ → 🔶 Отчёт готов, правки не внесены  
**Файлы прототипа:** `прототип.html` → `#screen-0`  
**Файлы live-бота:** `features/onboarding/SplashPage.tsx`, `SplashRive.tsx`

---

## 1. Общая концепция

### Прототип
Полноэкранный Splash с фоновым изображением (base64 webp — тёмное 3D-изображение сферы/кристалла), поверх которого по центру расположен логотип "AX" в круге с подписью "Automation · AI · Optimization". Никаких кнопок — экран чисто брендовый / загрузочный.

### Live-бот
Splash-экран с MjImage фоном (opacity 0.36), Rive-анимацией (или CSS-fallback "AX" в круге), кнопкой "Пропустить" вверху справа, подписью "Automation · AI · Optimization" и текстом "Загрузка Mini App shell..." внизу. Автоматический переход на `/welcome` через 2.2 сек.

### Вердикт
**Концептуально близки**, но есть расхождения в деталях UI и поведении.

---

## 2. Поэлементное сравнение

### 2.1 Фон

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Тип | Fullscreen webp-изображение (3D сфера/кристалл) | MjImage gradient-фон с opacity 0.36 | ⚠️ Расхождение |
| CSS | `background-size: cover; background-position: center` | `position: absolute; inset: 0; opacity: 0.36` | ⚠️ |
| z-index | 0 (`.fullscreen-bg`) | стиль `position: absolute` | ✅ OK |
| Затемнение | Нет overlay — контент поверх (`.content-overlay z-index:1`) | Нет overlay, MjImage с `scrim={false}` | ✅ Совпадает |

**Замечание:** В прототипе используется конкретная webp-текстура (тёмная 3D-сфера с teal-подсветкой). В live-боте — MjImage с id="splash-bg", который даёт CSS-градиент (т.к. реальных изображений нет). Нужно либо добавить реальное изображение, либо создать CSS-имитацию, максимально приближённую к прототипу.

---

### 2.2 Логотип "AX"

| Параметр | Прототип | Live-бот (CSS fallback) | Статус |
|----------|----------|-------------------------|--------|
| Контейнер | `130px × 130px`, `border-radius: 50%` | `130px × 130px`, `border-radius: 50%` | ✅ |
| Граница контейнера | `1.5px solid rgba(34,211,238,0.3)` | `1.5px solid rgba(34,211,238,0.3)` | ✅ |
| Шрифт текста "AX" | `'SF Mono', Consolas, monospace` | `'SF Mono', 'Fira Code', Consolas, monospace` | ✅ (незначительно) |
| Font-size | `34px` | `38px` | ⚠️ Расхождение |
| Font-weight | `700` | `700` | ✅ |
| Цвет текста | `#22D3EE` | `#22d3ee` | ✅ |
| Text-shadow | `0 0 20px rgba(34,211,238,0.3)` | `0 0 20px rgba(34,211,238,0.3)` | ✅ |
| Анимация | Нет (статичный) | `splashPulse 2.5s ease-in-out infinite` (scale + glow) | ⚠️ Добавлен |
| Rive-анимация | Нет | Предусмотрена, но `src = null` → CSS fallback | ℹ️ |

**Рекомендации:**
- Уменьшить `font-size` с 38px → 34px в `.splash-logo-fallback::after`
- Анимация `splashPulse` — допустимое улучшение, но если нужно 100% соответствие, убрать

---

### 2.3 Подпись "Automation · AI · Optimization"

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| margin-top | `16px` | Определяется gap контейнера (`gap: 20`) | ⚠️ |
| font-size | `12px` | Класс `p muted` → зависит от typography.css | ⚠️ Проверить |
| color | `rgba(240,246,252,0.35)` | Класс `muted` → `var(--app-text-muted)` | ⚠️ Проверить значение |
| letter-spacing | `3px` | Не задан inline | ❌ Отсутствует |
| font-family | `'SF Mono', monospace` | Наследует system-ui (не monospace) | ❌ Расхождение |

**Рекомендации:**
- Добавить `letter-spacing: 3px`
- Сменить шрифт на `'SF Mono', monospace`
- Уточнить `font-size: 12px` и `color: rgba(240,246,252,0.35)`

---

### 2.4 Дополнительные элементы

| Элемент | Прототип | Live-бот | Статус |
|---------|----------|----------|--------|
| Кнопка "Пропустить" | **Нет** | Есть (ghost, верхний правый угол) | ❌ Лишний элемент |
| Текст "Загрузка Mini App shell..." | **Нет** | Есть (внизу) | ❌ Лишний элемент |
| Авто-переход | Не определён в прототипе | `setTimeout 2200ms → /welcome` | ℹ️ Логика поведения |

**Рекомендации:**
- Удалить кнопку "Пропустить" (в прототипе нет интерактива на Splash)
- Удалить текст "Загрузка Mini App shell..."
- Авто-переход допустим как логика MiniApp, не противоречит прототипу

---

### 2.5 Шрифт (глобально для Splash)

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| font-family (body) | `'Montserrat', -apple-system, sans-serif` | `system-ui, -apple-system, 'SF Pro Text', Inter, Roboto, sans-serif` | ❌ Расхождение |
| Подключение | Google Fonts: Montserrat 300–700 | Системный шрифт | ❌ |

**Замечание:** Прототип использует **Montserrat** (Google Fonts) с весами 300, 400, 500, 600, 700. Live-бот использует системные шрифты. Это глобальное расхождение, влияющее на все экраны.

**Рекомендация:** Подключить Montserrat через Google Fonts или вебшрифт.

---

### 2.6 Лейаут / Позиционирование

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Компоновка | Контент по центру (flex, center/center) | 3 секции: верх (Пропустить), центр (логотип), низ (загрузка) | ⚠️ Расхождение |
| Высота | 100% экрана (812px в рамке телефона) | `min-height: 100dvh` | ✅ OK |
| Контент | Только логотип + подпись, вертикально по центру | Логотип + подпись по центру + лишние элементы сверху/снизу | ⚠️ |

**Рекомендации:**
- Убрать верхнюю и нижнюю секции
- Оставить единственную центрированную секцию с логотипом и подписью

---

## 3. Status Bar (Заголовок Telegram MiniApp)

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Высота | 48px | Управляется Telegram SDK | ℹ️ |
| Фон | `rgba(5,10,15,0.5)` + `blur(10px)` | Telegram-нативный | ℹ️ |
| Элементы | ✕ (слева), Badge "Axentrait" (центр), ••• (справа) | Telegram-нативные | ℹ️ |
| App Icon | 18px circle, gradient `#22D3EE → #06B6D4`, буква "A" | Telegram-нативный | ℹ️ |

**Замечание:** Status bar в Telegram MiniApp управляется платформой. Не является объектом правок в коде приложения.

---

## 4. Сводка расхождений

### Критические (❌)
1. **Кнопка "Пропустить"** — отсутствует в прототипе, нужно удалить
2. **Текст "Загрузка Mini App shell..."** — отсутствует в прототипе, нужно удалить
3. **letter-spacing: 3px** на подписи — отсутствует в live-боте
4. **font-family подписи** — должен быть `'SF Mono', monospace`, а не системный
5. **Глобальный шрифт Montserrat** — не подключен (влияет на все экраны)

### Средние (⚠️)
6. **font-size логотипа** — 38px вместо 34px
7. **Фоновое изображение** — градиент вместо конкретной текстуры 3D-сферы
8. **Анимация пульсации** — добавлена в live-боте, отсутствует в прототипе
9. **Отступ подписи** — `gap: 20` вместо `margin-top: 16px`

### Информационные (ℹ️)
10. Rive-анимация предусмотрена, но отключена (`null`)
11. Авто-переход 2.2 сек — логика MiniApp, допустима
12. Status bar — контролируется Telegram SDK

---

## 5. План правок (SplashPage.tsx)

### Шаг 1: Удалить лишние элементы
```diff
- <div className="ax-row" style={{ justifyContent: 'flex-end', zIndex: 1 }}>
-   <Button variant="ghost" onClick={() => navigate('/welcome')}>
-     Пропустить
-   </Button>
- </div>

- <div className="ax-col" style={{ gap: 6, textAlign: 'center', zIndex: 1 }}>
-   <span className="ax-muted">Загрузка Mini App shell...</span>
- </div>
```

### Шаг 2: Исправить подпись
```diff
- <p className="p muted">Automation · AI · Optimization</p>
+ <p style={{
+   fontSize: '12px',
+   color: 'rgba(240,246,252,0.35)',
+   letterSpacing: '3px',
+   fontFamily: "'SF Mono', Consolas, monospace"
+ }}>Automation · AI · Optimization</p>
```

### Шаг 3: Исправить font-size в global.css
```diff
  .splash-logo-fallback::after {
    content: 'AX';
    font-family: 'SF Mono', 'Fira Code', Consolas, monospace;
-   font-size: 38px;
+   font-size: 34px;
    font-weight: 700;
```

### Шаг 4: (Глобально) Подключить Montserrat
В `index.html`:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
```

В `tokens.css` / `global.css`:
```diff
- --ax-font-sans: system-ui, -apple-system, "SF Pro Text", Inter, Roboto, Arial, sans-serif;
+ --ax-font-sans: 'Montserrat', system-ui, -apple-system, "SF Pro Text", Inter, Roboto, Arial, sans-serif;
```

### Шаг 5: Упростить layout
```tsx
<main className="ax-shell" style={{ justifyContent: 'center', alignItems: 'center', position: 'relative', overflow: 'hidden' }}>
  <MjImage id="splash-bg" scrim={false} borderRadius={0} alt="splash-bg"
    style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', opacity: 0.36 }} />
  
  <section className="ax-col" style={{ alignItems: 'center', justifyContent: 'center', gap: 16, zIndex: 1 }}>
    <SplashRive />
    <p style={{
      fontSize: '12px',
      color: 'rgba(240,246,252,0.35)',
      letterSpacing: '3px',
      fontFamily: "'SF Mono', Consolas, monospace"
    }}>Automation · AI · Optimization</p>
  </section>
</main>
```

---

## 6. CSS-токены экрана (из прототипа)

```css
/* Splash Screen Tokens */
--splash-bg: url(base64...);           /* 3D sphere/crystal dark image */
--splash-logo-size: 130px;
--splash-logo-border: 1.5px solid rgba(34,211,238,0.3);
--splash-logo-font: 'SF Mono', Consolas, monospace;
--splash-logo-font-size: 34px;
--splash-logo-font-weight: 700;
--splash-logo-color: #22D3EE;
--splash-logo-shadow: 0 0 20px rgba(34,211,238,0.3);
--splash-tagline-font: 'SF Mono', monospace;
--splash-tagline-font-size: 12px;
--splash-tagline-color: rgba(240,246,252,0.35);
--splash-tagline-spacing: 3px;
--splash-tagline-margin-top: 16px;
```
