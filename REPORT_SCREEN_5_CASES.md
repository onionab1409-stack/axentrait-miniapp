# Отчёт: Экран 5 — Cases (SCR-CS-001)

**Дата:** 2026-02-25  
**Статус:** ⬜ → 🔶 Отчёт готов, правки не внесены  
**Файлы прототипа:** `прототип.html` → `#screen-5`  
**Файлы live-бота:** `features/cases/CasesGalleryPage.tsx`, `components/domain/CaseCard.tsx`, `components/ui/Chip.tsx`

---

## 1. Общая концепция

### Прототип
Тёмный фон (`#050A0F`). Структура:
1. **H2** — "Кейсы" (26px, weight 300, cyan)
2. **Chip-фильтры** — горизонтальный скролл: "Все" (active, cyan bg), "E-commerce", "Производство", "SaaS" (inactive, border)
3. **Карточки кейсов** — mj-card (200px height, background-image cover, одно-строчный title)
4. **Bottom Navigation** — 4 элемента, "Кейсы" active

Карточки предельно простые: только заголовок-результат (16px, weight 300, #7EE8F2) поверх fullscreen-изображения. **Нет:** industry label, metrics.headline (цветной), tag-чипов внутри карточки.

### Live-бот
AppShell "Кейсы" + showBottomNav. Структура:
1. **Без H2** — заголовок в TopBar
2. **Chip-фильтры** — "Все" + 7 отраслей (E-commerce, Производство, SaaS, Медтех, Логистика, Ритейл, Финтех)
3. **CaseCard** — MjImage (180px) с industry label, title (16px bold white), metrics.headline (cyan), tag-чипы
4. **Bottom Navigation** — 5 элементов, "Кейсы" active

### Вердикт
**Структурно близко**, это один из наиболее соответствующих экранов. Основные расхождения — в стилистике текста (weight, color), содержимом карточек (лишние элементы) и H2.

---

## 2. Поэлементное сравнение

### 2.1 Лейаут

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| TopBar | **Нет** (заголовок внутри контента) | "Кейсы" (TopBar) | ❌ |
| H2 заголовок | **Есть** внутри контента | **Нет** (только TopBar title) | ❌ |
| Фон | `#050A0F` (solid dark) | Body gradients | ⚠️ Близко |
| Padding | `64px 20px 80px` | Shell + chip row padding | ⚠️ |
| Bottom Nav | Есть (4 элемента) | Есть (5 элементов) | ⚠️ |
| Scroll | `overflow-y: auto` | ax-shell scroll | ✅ |

---

### 2.2 Заголовок

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Расположение | **Внутри контента** (h2 тег) | **В TopBar** (AppShell title) | ❌ |
| font-size | `26px` | TopBar: ~17px bold | ❌ |
| font-weight | **`300`** | **`700`** (TopBar) | ❌ |
| color | **`#7EE8F2`** | **`#f0f6fc`** (white) | ❌ |
| text-shadow | `0 0 30px rgba(34,211,238,0.2)` | `none` | ❌ |
| margin-bottom | `16px` | — | — |

**Рекомендация:** Добавить H2 заголовок в тело страницы (как в прототипе), либо стилизовать TopBar.

---

### 2.3 Chip-фильтры

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Layout | `display:flex; gap:8px; overflow-x:auto` | `className="ax-row"; gap:8px; overflowX:auto` | ✅ |
| margin-bottom | `20px` | `paddingBottom: 2` | ⚠️ |

#### Active chip ("Все")

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| background | `rgba(34,211,238,0.12)` | `rgba(34,211,238,0.12)` | ✅ |
| color | `#22D3EE` | `#22D3EE` | ✅ |
| border | `1px solid rgba(34,211,238,0.2)` | `1px solid rgba(34,211,238,0.2)` | ✅ |
| font-size | `11px` | `12px` | ⚠️ |
| padding | `4px 10px` | `6px 12px` | ⚠️ Крупнее |
| border-radius | `8px` | `8px` | ✅ |
| backdrop-filter | `blur(4px)` | Нет | ⚠️ |
| minHeight | Нет | `36px` | ⚠️ Крупнее |

#### Inactive chip

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| background | Нет (transparent) | `rgba(255,255,255,0.04)` | ⚠️ |
| color | `rgba(126,232,242,0.5)` (teal muted) | `rgba(240,246,252,0.65)` (gray) | ⚠️ |
| border | `1px solid rgba(126,232,242,0.15)` (teal) | `1px solid rgba(255,255,255,0.06)` (gray) | ⚠️ |

**Chip-фильтры — одна из наиболее близких частей.** Различия только в размерах (прототип компактнее) и цвете inactive (teal vs gray).

---

### 2.4 Карточки кейсов

| Параметр | Прототип (mj-card) | Live (CaseCard) | Статус |
|----------|---------------------|-----------------|--------|
| **Класс** | `mj-card` | `<button>` + `MjImage` | ⚠️ |
| **height** | **`200px`** | **`180px`** | ⚠️ |
| margin-bottom | `14px` | `gap: 12px` (от контейнера) | ⚠️ Близко |
| border-radius | `18px` | `16px` | ⚠️ |
| border | `1px solid rgba(255,255,255,0.06)` | `1px solid var(--app-border)` | ✅ Близко |
| background | `background-image: url(...)` cover | MjImage component | ✅ Структурно |
| padding | `16px` (от mj-card class) | Через MjImage | ⚠️ |

#### Содержимое карточки

| Элемент | Прототип | Live-бот | Статус |
|---------|----------|----------|--------|
| **Industry label** | **Нет** | **Есть** — 11px, rgba(240,246,252,0.45) | ❌ Лишний |
| **Title** | Есть — результат кейса | Есть — полный title | ✅ Содержимое |
| Title font-size | `16px` | `16px` | ✅ |
| **Title font-weight** | **`300`** | **`700`** | ❌ |
| Title line-height | `1.25` | `1.25` | ✅ |
| **Title letter-spacing** | `0.5px` | `normal` | ⚠️ |
| **Title color** | **`#7EE8F2`** (cyan) | **`#fff`** (white) | ❌ |
| **metrics.headline** | **Нет** | **Есть** — 14px, #22D3EE, fontWeight 600 | ❌ Лишний |
| **Tag chips** | **Нет** | **Есть** — 11px, border chips (3 шт.) | ❌ Лишний |

**Карточки кейсов в прототипе максимально минималистичны:** только заголовок-результат (тонкий, cyan) поверх изображения. Live добавляет industry label, metrics headline (зелёный) и tag-чипы, делая карточки информационно перегруженными по сравнению с эталоном.

---

### 2.5 Данные кейсов

| # | Прототип (title) | Live (title) | Статус |
|---|------------------|-------------|--------|
| 1 | "−55% времени на отчётность за 10 недель" | "−55% времени на отчётность за 10 недель в e-commerce (100K+ заказов)" | ⚠️ Длиннее |
| 2 | "−4,5 п.п. простоев за 14 недель" | "−4,5 п.п. простоев линии за 14 недель на перерабатывающем заводе" | ⚠️ Длиннее |
| 3 | "CI/CD за 6 недель: релизы ×3" | "CI/CD за 6 недель: релизы ×3 в SaaS-стартапе" | ⚠️ Длиннее |
| 4 | "−42% ручного ввода за 7 недель" | "−42% ручного ввода за 7 недель в логистической компании" | ⚠️ Длиннее |
| 5 | "−58% уязвимостей за 10 недель" | "−58% критических уязвимостей за 10 недель в финтехе" | ⚠️ Длиннее |
| 6 | — | "−31% времени обработки обращений за 9 недель в медтехе" | ⚠️ Новый |
| 7 | — | "−57% времени возвратов за 2 месяца в ритейл-сети" | ⚠️ Новый |

Live-бот содержит 7 кейсов vs 5 в прототипе. Titles в live длиннее (добавлен контекст отрасли).

---

### 2.6 Bottom Navigation

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Наличие | ✅ | ✅ | ✅ |
| Active tab | "Кейсы" (active) | "Кейсы" (active) | ✅ |
| Количество | **4** (Услуги, Кейсы, AI-демо, Заявка) | **5** (+Профиль) | ⚠️ |

---

## 3. Сводка расхождений

### Критические (❌)
1. **H2 "Кейсы" отсутствует в body** — прототип имеет большой cyan заголовок в контенте, live — только TopBar
2. **Title font-weight: 700 → 300** в карточках
3. **Title color: #fff → #7EE8F2** в карточках
4. **Лишние элементы в карточках:** industry label, metrics.headline, tag chips

### Средние (⚠️)
5. Chip размеры: live крупнее (12px/36px vs 11px/auto)
6. Chip inactive цвет: gray vs teal
7. Chip backdrop-filter отсутствует в live
8. Card height: 180px vs 200px
9. Card border-radius: 16px vs 18px
10. Title letter-spacing: normal vs 0.5px
11. Titles длиннее в live (+ контекст отрасли)
12. 7 кейсов vs 5
13. Bottom Nav: 5 vs 4 элемента

### Совпадения (✅)
- Chip-фильтры — структурно идентичны, цвета active совпадают
- Общая концепция: chip-ряд + вертикальный список карточек
- Bottom Nav — стилистически очень близок
- Card background-image подход
- Title font-size: 16px совпадает

---

## 4. План правок

### Шаг 1: Добавить H2 в тело страницы
```tsx
// CasesGalleryPage.tsx — после AppShell
<h2 style={{
  fontSize: 26,
  fontWeight: 300,
  marginBottom: 16,
  letterSpacing: '0.5px',
  color: '#7EE8F2',
  textShadow: '0 0 30px rgba(34,211,238,0.2)',
}}>
  Кейсы
</h2>
```

### Шаг 2: CaseCard — упрощение
```tsx
// CaseCard.tsx — убрать лишние элементы
<MjImage id={imageId} height={200} borderRadius={0} alt={caseStudy.title}>
  {/* УБРАТЬ: industry label */}
  <div style={{
    fontSize: 16,
    fontWeight: 300,       // было 700
    lineHeight: 1.25,
    letterSpacing: '0.5px',
    color: '#7EE8F2',      // было #fff
  }}>
    {caseStudy.title}
  </div>
  {/* УБРАТЬ: metrics.headline */}
  {/* УБРАТЬ: tag chips */}
</MjImage>
```

### Шаг 3: Card styling
```tsx
// CaseCard outer button
style={{
  border: '1px solid rgba(255,255,255,0.06)',
  borderRadius: 18,      // было 16
  overflow: 'hidden',
  background: 'transparent',
  padding: 0,
  textAlign: 'left',
  cursor: 'pointer',
}}
```

### Шаг 4: Chip — компактнее
```tsx
// Chip.tsx — уменьшить до прототипа
style={{
  fontSize: 11,          // было 12
  padding: '4px 10px',   // было 6px 12px
  minHeight: 'auto',     // убрать 36px
  backdropFilter: 'blur(4px)',
  // inactive:
  color: active ? '#22D3EE' : 'rgba(126,232,242,0.5)',    // teal вместо gray
  border: `1px solid ${active ? 'rgba(34,211,238,0.2)' : 'rgba(126,232,242,0.15)'}`, // teal border
  background: active ? 'rgba(34,211,238,0.12)' : 'transparent',
}}
```

### Шаг 5: margin-bottom на chip-row
```tsx
<div className="ax-row" style={{ gap: 8, overflowX: 'auto', marginBottom: 20 }}>
```

---

## 5. CSS-токены экрана (из прототипа)

```css
/* Cases Screen Tokens */
--cases-bg: #050A0F;
--cases-padding: 64px 20px 80px;

--cases-h2-size: 26px;
--cases-h2-weight: 300;
--cases-h2-color: #7EE8F2;
--cases-h2-spacing: 0.5px;
--cases-h2-shadow: 0 0 30px rgba(34,211,238,0.2);
--cases-h2-margin-bottom: 16px;

/* Chip filter */
--chip-size: 11px;
--chip-padding: 4px 10px;
--chip-radius: 8px;
--chip-blur: blur(4px);
--chip-active-bg: rgba(34,211,238,0.12);
--chip-active-color: #22D3EE;
--chip-active-border: 1px solid rgba(34,211,238,0.2);
--chip-inactive-bg: transparent;
--chip-inactive-color: rgba(126,232,242,0.5);
--chip-inactive-border: 1px solid rgba(126,232,242,0.15);
--chip-row-gap: 8px;
--chip-row-margin-bottom: 20px;

/* Case card (mj-card) */
--case-card-height: 200px;
--case-card-radius: 18px;
--case-card-border: 1px solid rgba(255,255,255,0.06);
--case-card-padding: 16px;
--case-card-margin-bottom: 14px;
--case-card-bg: background-image cover;

--case-title-size: 16px;
--case-title-weight: 300;
--case-title-line-height: 1.25;
--case-title-spacing: 0.5px;
--case-title-color: #7EE8F2;
/* NO: industry label, metrics headline, tags */
```

---

## 6. Оценка близости

**Этот экран — один из наиболее близких к прототипу.** Общая структура (chip filters + card list + bottom nav) совпадает. Расхождения в основном стилистические:
- Стандартный паттерн weight 700→300, white→cyan
- Информационная перегрузка карточек (лишние элементы)
- Chip sizing (мелкие различия)

Оценка: ~60% соответствия (лучший показатель среди всех экранов).
