# Отчёт: Экран 1 — Welcome (SCR-ONB-010)

**Дата:** 2026-02-25  
**Статус:** ⬜ → 🔶 Отчёт готов, правки не внесены  
**Файлы прототипа:** `прототип.html` → `#screen-1`  
**Файлы live-бота:** `features/onboarding/WelcomePage.tsx`, `components/ui/Button.tsx`, `components/ui/SplineScene.tsx`, `components/ui/MjImage.tsx`

---

## 1. Общая концепция

### Прототип
Полноэкранный Welcome с фоновым изображением (3D абстрактная фигура — металлическая/углеродная лента на тёмном фоне). Поверх фона — заголовок `h1` вверху (cyan цвет, тонкий шрифт weight 300) и две кнопки внизу: "Подобрать решение" (полупрозрачная primary) и "Смотреть кейсы" (компактная secondary по центру). Контент распределён по вертикали через `justify-content: space-between`.

### Live-бот
Полноэкранный Welcome с фоном через SplineScene (отключён → fallback MjImage `hero-main.webp`) + darken overlay gradient. Заголовок `h1` белый (не cyan), weight 700 (не 300). Кнопки внизу: "Подобрать решение" (яркий gradient primary, fullWidth) и "Смотреть кейсы" (secondary, fullWidth).

### Вердикт
**Структурно совпадают**, но значительные расхождения в стилистике заголовка, кнопок и общей визуальной подаче.

---

## 2. Поэлементное сравнение

### 2.1 Фон

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Тип | Fullscreen webp-изображение (3D абстракция) | `hero-main.webp` (74KB, реальный файл) | ✅ Есть изображение |
| Overlay | Нет (контент напрямую поверх bg) | `linear-gradient(180deg, rgba(5,10,15,0.45) 0%, rgba(5,10,15,0.25) 40%, rgba(5,10,15,0.7) 75%, rgba(5,10,15,0.92) 100%)` | ⚠️ Добавлен overlay |
| Позиция bg | `background-size: cover; background-position: center` | MjImage с `backgroundSize: cover` | ✅ OK |
| Spline 3D | Нет | Предусмотрен, `splineUrl = null` → fallback | ℹ️ |

**Замечание:** Overlay в live-боте делает нижнюю часть экрана значительно темнее, чем в прототипе. В прототипе фон виден полностью без затемнения. Однако overlay улучшает читаемость — это **допустимое расхождение**, если фоновое изображение в live совпадает с прототипом.

**Рекомендация:** Проверить визуальное соответствие `hero-main.webp` фоновому изображению прототипа. При необходимости ослабить overlay (уменьшить opacity).

---

### 2.2 Заголовок H1

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Текст | "Вы в двух минутах от лучшей версии вашего бизнеса" | "Вы в двух минутах от лучшей версии вашего бизнеса" | ✅ |
| font-size | `28px` | `28px` | ✅ |
| font-weight | **`300`** (тонкий, лёгкий) | **`700`** (жирный) | ❌ Критическое расхождение |
| line-height | `1.15` | `1.25` (= 35px) | ⚠️ Расхождение |
| color | **`#7EE8F2`** (cyan/teal) | **`#fff`** (белый) | ❌ Критическое расхождение |
| letter-spacing | `0.5px` | `normal` | ⚠️ Расхождение |
| text-shadow | `0 0 30px rgba(34,211,238,0.2)` | `none` | ❌ Расхождение |
| text-align | `center` | `center` | ✅ |
| max-width | Не задан (текст переносится `<br>`) | `340px` | ⚠️ Разный механизм переноса |
| padding-top | `58px (overlay padding) + 7vh` | `60px paddingTop` | ✅ Близко |
| Перенос строк | Принудительный через `<br>`: "Вы в двух минутах⏎от лучшей версии⏎вашего бизнеса" | Автоматический по maxWidth 340px | ⚠️ Визуально может отличаться |
| font-family | Montserrat (глобально) | system-ui (глобально) | ❌ (из отчёта Screen 0) |

**Это ключевое визуальное расхождение.** В прототипе заголовок — cyan, тонкий, воздушный (weight 300 + letter-spacing + text-shadow). В live-боте — белый, жирный, плотный (weight 700). Совершенно разный визуальный характер.

---

### 2.3 Кнопка "Подобрать решение" (Primary)

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Текст | "Подобрать решение" | "Подобрать решение" | ✅ |
| width | `100%` (класс `btn-primary`) | `100%` (fullWidth) | ✅ |
| padding | `15px 0` | `16px 32px` | ⚠️ Минимально |
| border-radius | `14px` | `14px` | ✅ |
| **background** | **`rgba(34,211,238,0.15)`** (полупрозрачный!) | **`linear-gradient(135deg, #22D3EE 0%, #06B6D4 50%, #0891B2 100%)`** (яркий solid gradient!) | ❌ Критическое расхождение |
| **color** | **`#22D3EE`** (cyan текст) | **`#050A0F`** (тёмный текст на ярком фоне) | ❌ Критическое расхождение |
| border | `none` | `none` | ✅ |
| box-shadow | `none` (переопределён inline) | `0 4px 16px rgba(34,211,238,0.25), inset 0 1px 0 rgba(255,255,255,0.15)` | ❌ Лишний |
| font-size | `15px` | `16px` | ⚠️ Минимально |
| font-weight | `600` | `600` | ✅ |
| margin-bottom | `12px` | gap=12px (от контейнера) | ✅ |

**Критическая разница:** В прототипе primary-кнопка на Welcome **НЕ** использует стандартный яркий gradient. Вместо этого она полупрозрачная (`rgba(34,211,238,0.15)`) с cyan текстом — это glass-morphism стиль, сквозь кнопку видно фон. В live-боте используется стандартный непрозрачный gradient Button variant="primary".

**Рекомендация:** Создать отдельный вариант кнопки или переопределить стиль inline. Это не стандартный `primary`, а скорее `glass-primary` или `welcome-primary`.

---

### 2.4 Кнопка "Смотреть кейсы" (Secondary)

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Текст | "Смотреть кейсы" | "Смотреть кейсы" | ✅ |
| **width** | **Auto (padding-based)** — по размеру текста, центрирована | **100% (fullWidth)** | ❌ Расхождение |
| padding | `11px 22px` | `16px 32px` | ⚠️ Более крупная |
| border-radius | `12px` | `14px` | ⚠️ Минимально |
| background | `transparent` | `rgba(34,211,238,0.08)` | ⚠️ Полупрозрачный vs transparent |
| color | `rgba(34,211,238,0.6)` (приглушённый cyan) | `rgb(34,211,238)` (#22D3EE — яркий cyan) | ⚠️ Расхождение |
| border | `1px solid rgba(34,211,238,0.15)` (очень тонкая) | `1px solid rgba(34,211,238,0.2)` | ⚠️ Близко |
| font-size | `13px` | `16px` | ⚠️ Крупнее |
| font-weight | `500` | `600` | ⚠️ Жирнее |
| Позиция | **Центрирована** (flex justify-content:center wrapper) | **Растянута на всю ширину** | ❌ Расхождение |
| min-height | Не задан (по padding) | `52px` | ⚠️ |

**Замечание:** В прототипе кнопка "Смотреть кейсы" — компактная, центрированная, визуально лёгкая. В live-боте — полноразмерная, визуально тяжёлая, выглядит как вторая primary.

---

### 2.5 Лейаут и позиционирование

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Layout | `justify-content: space-between` | `flex: 1` (заголовок) + кнопки внизу | ✅ Концептуально |
| Padding container | `0 20px 40px`, padding-top: 58px | `0 24px`, paddingBottom: 48px | ⚠️ Близко |
| Кнопки снизу | Кнопки прижаты к низу | Кнопки прижаты к низу | ✅ |
| Gap между кнопками | `12px` (margin-bottom на primary) | `12px` (gap) | ✅ |
| Выравнивание secondary | **flex justify-content: center** (по центру) | fullWidth (на всю ширину) | ❌ |

---

### 2.6 Фоновое изображение — визуальное сравнение

| Параметр | Прототип | Live-бот | Статус |
|----------|----------|----------|--------|
| Текстура | 3D абстрактная фигура — металлическая/углеродная скрученная лента, teal-подсветка, тёмный фон | `hero-main.webp` — требует визуальной проверки | ⚠️ Проверить |
| Покрытие экрана | 100% без overlay | 100% + darken overlay | ⚠️ |
| Затемнение снизу | Естественное затенение от изображения | 4-stop gradient overlay (0.45→0.25→0.7→0.92) | ⚠️ |

---

## 3. Сводка расхождений

### Критические (❌)
1. **H1 font-weight: 700 → 300** — жирный шрифт вместо тонкого, меняет весь характер
2. **H1 color: #fff → #7EE8F2** — белый вместо cyan, теряется фирменный стиль
3. **H1 text-shadow отсутствует** — `0 0 30px rgba(34,211,238,0.2)` даёт мягкое свечение
4. **Primary кнопка: solid gradient → полупрозрачная** — в прототипе glass-morphism (`rgba(34,211,238,0.15)`), в live — непрозрачный gradient. Полностью разный визуал.
5. **Primary кнопка color: dark → cyan** — текст кнопки должен быть `#22D3EE`, не `#050A0F`
6. **Secondary кнопка fullWidth → auto (по центру)** — должна быть компактной и центрированной
7. **Глобальный шрифт Montserrat** — не подключен (см. отчёт Screen 0)

### Средние (⚠️)
8. **H1 line-height: 1.25 → 1.15** — более плотный межстрочный интервал
9. **H1 letter-spacing: normal → 0.5px** — небольшое разрежение букв
10. **Secondary font-size: 16px → 13px** — должна быть мельче
11. **Secondary font-weight: 600 → 500** — чуть легче
12. **Secondary color: яркий cyan → приглушённый** — `rgba(34,211,238,0.6)` vs `#22D3EE`
13. **Secondary border-radius: 14px → 12px**
14. **Darken overlay на фоне** — добавлен в live, отсутствует в прототипе
15. **Primary box-shadow** — есть в live, нет в прототипе (inline переопределён на `none`)
16. **Primary padding** — `15px 0` vs `16px 32px`

### Информационные (ℹ️)
17. Spline 3D предусмотрен, но отключён (`null`)
18. Telegram MainButton отключён (`visible: false`)
19. `useOnboardingState.reset()` вызывается при старте — логика

---

## 4. План правок

### Шаг 1: Исправить H1 (WelcomePage.tsx)

```diff
  <h1
    style={{
-     fontSize: 28,
-     fontWeight: 700,
-     lineHeight: 1.25,
-     color: '#fff',
+     fontSize: 28,
+     fontWeight: 300,
+     lineHeight: 1.15,
+     color: '#7EE8F2',
      textAlign: 'center',
      margin: 0,
      maxWidth: 340,
+     letterSpacing: '0.5px',
+     textShadow: '0 0 30px rgba(34,211,238,0.2)',
    }}
  >
```

### Шаг 2: Исправить Primary кнопку

Вариант A — inline style override:
```tsx
<Button
  variant="primary" size="lg" fullWidth
  onClick={startOnboarding}
  style={{
    background: 'rgba(34,211,238,0.15)',
    color: '#22D3EE',
    boxShadow: 'none',
    border: 'none',
  }}
>
  {welcome.ctaButton}
</Button>
```

Вариант B — создать новый variant `glassPrimary` в Button.tsx:
```tsx
glassPrimary: {
  background: 'rgba(34,211,238,0.15)',
  color: '#22D3EE',
  border: 'none',
  boxShadow: 'none',
},
```

### Шаг 3: Исправить Secondary кнопку

```diff
- <Button variant="secondary" size="lg" fullWidth onClick={openCases}>
-   Смотреть кейсы
- </Button>
+ <div style={{ display: 'flex', justifyContent: 'center' }}>
+   <Button
+     variant="secondary"
+     onClick={openCases}
+     style={{
+       padding: '11px 22px',
+       borderRadius: 12,
+       fontSize: 13,
+       fontWeight: 500,
+       color: 'rgba(34,211,238,0.6)',
+       borderColor: 'rgba(34,211,238,0.15)',
+       background: 'transparent',
+       minHeight: 'auto',
+     }}
+   >
+     Смотреть кейсы
+   </Button>
+ </div>
```

### Шаг 4: Ослабить overlay (опционально)

Если фоновое изображение визуально совпадает с прототипом, рассмотреть уменьшение затемнения:
```diff
  background:
-   'linear-gradient(180deg, rgba(5,10,15,0.45) 0%, rgba(5,10,15,0.25) 40%, rgba(5,10,15,0.7) 75%, rgba(5,10,15,0.92) 100%)',
+   'linear-gradient(180deg, rgba(5,10,15,0.3) 0%, rgba(5,10,15,0.1) 40%, rgba(5,10,15,0.5) 75%, rgba(5,10,15,0.8) 100%)',
```

---

## 5. CSS-токены экрана (из прототипа)

```css
/* Welcome Screen Tokens */
--welcome-bg: url(base64...);                   /* 3D abstract figure */
--welcome-h1-font-size: 28px;
--welcome-h1-font-weight: 300;
--welcome-h1-line-height: 1.15;
--welcome-h1-color: #7EE8F2;
--welcome-h1-letter-spacing: 0.5px;
--welcome-h1-text-shadow: 0 0 30px rgba(34,211,238,0.2);

--welcome-primary-bg: rgba(34,211,238,0.15);
--welcome-primary-color: #22D3EE;
--welcome-primary-border: none;
--welcome-primary-shadow: none;
--welcome-primary-radius: 14px;
--welcome-primary-padding: 15px 0;
--welcome-primary-font-size: 15px;
--welcome-primary-font-weight: 600;

--welcome-secondary-bg: transparent;
--welcome-secondary-color: rgba(34,211,238,0.6);
--welcome-secondary-border: 1px solid rgba(34,211,238,0.15);
--welcome-secondary-radius: 12px;
--welcome-secondary-padding: 11px 22px;
--welcome-secondary-font-size: 13px;
--welcome-secondary-font-weight: 500;
--welcome-secondary-width: auto;                /* НЕ fullWidth */

--welcome-content-padding: 0 20px 40px;
--welcome-content-padding-top: calc(58px + 7vh);
--welcome-buttons-gap: 12px;
```

---

## 6. Визуальная иерархия — ключевое различие

В прототипе Welcome экран построен по принципу **премиальной минимальности**:
- Заголовок — тонкий, лёгкий, cyan с мягким glow (weight 300)
- Primary кнопка — полупрозрачная glass, не кричит
- Secondary кнопка — компактная, приглушённая, второстепенная
- Фон — главный визуальный элемент, не перекрыт

В live-боте стиль скорее **утилитарный/action-oriented**:
- Заголовок — жирный, белый, без glow (weight 700)
- Primary кнопка — яркий непрозрачный gradient, доминирует визуально
- Secondary — такого же размера как primary, визуально тяжёлая
- Фон — затемнён overlay

Для приведения к эталону нужно сместить баланс от "утилитарного" к "премиальному".
