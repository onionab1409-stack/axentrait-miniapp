/**
 * Screen -> Spline scene URL.
 * null means scene is not ready and fallback visual is used.
 */
export const SPLINE_SCENES: Record<string, string | null> = {
  splash: null,
  welcome: null,
  'ai-hub': null,
};
